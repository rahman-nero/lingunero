name: Testing workflow

on:
  push:
    branches:
      - v2

jobs:
  test:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: hello world
          run: echo "hello world"
          shell: bash

  test2:
      runs-on: ubuntu-latest
      steps:
        - name: good bye world
          run: echo "good bye"
          shell: bash
